name: GH Reusable workflow 

on:
  workflow_dispatch:
    inputs:
      name:
        type: choice
        description: Branch
        options: 
        - master
        - dev
        - feature
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    
    steps:
      - name: test 
        shell: bash 
        run: echo "Hello"
  
