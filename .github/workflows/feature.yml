name: Feature Reusable workflow 

on:
  workflow_call:
    inputs:
      var1:
        required: true
        type: string
        
    
jobs:
  called-workflow:
    runs-on: ubuntu-latest
    steps:
      - name: Print the  variables value
        run: |
          echo ${{ inputs.var1 }}
          
